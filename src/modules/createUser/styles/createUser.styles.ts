import styled from "styled-components/native";
import { theme } from "../../../shared/theme/theme";

export default {
    CreateUserContainer: styled.ScrollView`
        background-color: ${theme.colors.neutralTheme.white};
        padding: 16px;
        
    `
}